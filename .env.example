# В Docker используйте hostname "db" (имя сервиса в docker-compose.yml)
DATABASE_URL=postgresql+asyncpg://admin:superpassword@db:5432/business_db

# Для HS256 рекомендуется минимум 32 байта/символа
SECRET_KEY=change_me_to_a_random_32+_chars_secret_key